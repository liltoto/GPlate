import React from 'react'
import { shallow } from 'enzyme'
import Hello from './'

describe('<Hello />', () => {
	const wrapper = shallow(<Hello />)

	it('Stupid test DELETE ME') {
		expect(1).toEqual(1)
	})
}
