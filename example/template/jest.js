import React from 'react'
import { shallow } from 'enzyme'
import {{name}} from './'

describe('<{{name}} />', () => {
	const wrapper = shallow(<{{name}} />)

	it('Stupid test DELETE ME') {
		expect(1).toEqual(1)
	})
}
