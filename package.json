{
  "name": "gplate",
  "version": "0.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules",
    "build": "babel ./src -d ./lib",
    "prepublishOnly": "npm run clean && npm install && npm run build",
    "publish-fix": "npm version patch && npm publish",
    "publish-feature": "npm version minor && npm publish",
    "publish-breakable": "npm version major && npm publish"
  },
  "keywords": [],
  "author": "Güner Yildirim",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.1",
    "commander": "^2.19.0",
    "fs-extra": "^7.0.1",
    "handlebars": "^4.0.12",
    "inquirer": "^6.2.0",
    "js-yaml": "^3.12.0"
  },
  "bin": {
    "gplate": "./lib/index.js"
  },
  "engines": {
    "node": ">=8.13.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YouSee/tvlab-humax-pluto.git"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-minify": "^0.5.0"
  }
}
